# ── PyTorch — CUDA 12.4 wheels ────────────────────────────
# RTX 3090 is fully supported by PyTorch 2.3 + cu124
--index-url https://download.pytorch.org/whl/cu124
torch==2.3.1+cu124
torchvision==0.18.1+cu124

# ── Core ML / CV ───────────────────────────────────────────
--index-url https://pypi.org/simple
numpy==1.26.4
opencv-python-headless==4.10.0.84
Pillow==10.3.0
matplotlib==3.9.0
scipy==1.13.1
scikit-learn==1.5.0

# ── Training utils ─────────────────────────────────────────
tqdm==4.66.4
tensorboard==2.17.0
wandb==0.17.3           # optional: comment out if you don't use W&B yet

# ── ONNX / export pipeline ─────────────────────────────────
onnx==1.16.1
onnxruntime-gpu==1.18.1  # for PTQ / INT8 benchmarking on desktop
# onnxruntime==1.18.1    # swap this in for Jetson (CPU/TensorRT path instead)

# ── Jupyter ────────────────────────────────────────────────
jupyter==1.0.0
jupyterlab==4.2.3
ipywidgets==8.1.3

# ── Python depth (Fluent Python exercises) ─────────────────
# No extra installs needed — stdlib + above covers Ch 1-7 well

# ── Linting / formatting ───────────────────────────────────
black==24.4.2
isort==5.13.2
mypy==1.10.0

# ── Testing ────────────────────────────────────────────────
pytest==8.2.2
pytest-cov==5.0.0
